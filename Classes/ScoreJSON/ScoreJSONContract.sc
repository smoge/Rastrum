// ScoreJSONContract
//
// Keeps the active format tag and version here.
ScoreJSONContract {
    classvar <schemaFormat = "score-json";
    classvar <schemaVersion = 31;

    // >>> ScoreJSONContract.schemaFormat       -> score-json
    // >>> ScoreJSONContract.schemaVersion > 23 -> true
}
